apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels:
    app: plateforge
    app.kubernetes.io/name: plateforge
    app.kubernetes.io/part-of: plateforge
    app.kubernetes.io/component: backend
    component: backend
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: plateforge
      component: backend
  template:
    metadata:
      labels:
        app: plateforge
        app.kubernetes.io/name: plateforge
        app.kubernetes.io/part-of: plateforge
        app.kubernetes.io/component: backend
        component: backend
    spec:
      containers:
        - name: backend
          image: ghcr.io/aleckarfonta/electroplating/plateforge:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8116
              name: http
          envFrom:
            - configMapRef:
                name: plateforge-config
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
          volumeMounts:
            - name: sessions
              mountPath: /app/sessions
            - name: logs
              mountPath: /app/logs
      volumes:
        - name: sessions
          emptyDir: {}
        - name: logs
          emptyDir: {}
